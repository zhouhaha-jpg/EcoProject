{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22",
    "python": "3.12"
  },
  "steps": {
    "build": {
      "commands": ["...", "pip install --target=./.python_packages -r python/requirements.txt"]
    }
  }
}
